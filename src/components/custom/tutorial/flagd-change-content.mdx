Let's change the feature flag in our `flags.flagd.json`, making `defaultVariant` to `on`

```json
{
  "flags": {
    "welcome-message": {
      "variants": {
        "on": true,
        "off": false
      },
      "state": "ENABLED",
// diff-remove
      "defaultVariant": "off"
// diff-add
      "defaultVariant": "on"
    }
  }
}
```
